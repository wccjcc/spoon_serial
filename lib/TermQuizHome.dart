import 'package:flutter/material.dart';

class TermQuizPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("용어퀴즈풀기 페이지"),
      ),
      body: Center(
        child: Text("용어퀴즈풀기 페이지 내용"),
      ),
    );
  }
}