import 'package:flutter/material.dart';

class FeaturePracticePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("기능연습 페이지"),
      ),
      body: Center(
        child: Text("기능연습 페이지 내용"),
      ),
    );
  }
}